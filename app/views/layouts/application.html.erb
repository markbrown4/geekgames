<!DOCTYPE html>
<!--[if IE 7 ]> <html lang="en" class="no-js ie ie7"> <![endif]-->
<!--[if IE 8 ]> <html lang="en" class="no-js ie ie8"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html class="no-js" lang="en"> <!--<![endif]-->
<head>
<meta charset="utf-8">
<title>GeekGames</title>
<%= stylesheet_link_tag "application", :media => "all" %>
<!--[if IE]>
<%= stylesheet_link_tag "ie", :media => "all" %>
<![endif]-->
<%= csrf_meta_tags %>
</head>
<body>
<div class="navbar navbar-fixed-top">
	<div class="navbar-inner">
		<div class="container">
			<a class="brand" href="/">geekgames</a>
			<ul class="nav pull-left">
				<%- menu_items = ['home','games','leaderboard','prizes'] %>
				<%- menu_items.each do |page| %>
				<li<%= %( class="active").html_safe if menu == page %>><%= link_to t(page), "/#{page}" %></li>
				<%- end %>
			</ul>
			<% if user_signed_in? %>
			<ul class="nav pull-right">
				<li class="dropdown" id="profile">
					<a class="dropdown-toggle" data-toggle="dropdown" href="#profile"><%= gravatar_image_tag(current_user.email, :alt => current_user.username) %> <%= current_user.username %> <b class="caret"></b></a>
					<ul class="dropdown-menu">
						<li<%= ' class="active"'.html_safe if menu == "profile" %>><%= link_to "Profile", profile_path %></li>
						<li class="divider"></li>
						<li><%= link_to "Sign out", destroy_user_session_path, :method=>'delete' %></li>
					</ul>
				</li>
			</ul>
			<% end %>
		</div>
	</div>
</div>
<div id="wrapper" class="container">
	<%= render 'flashes' %>
	<div id="content">
		<%= yield %>
	</div>
	<ul id="footer">
		<li><%= link_to "Rules of the Game", page_path('rules') %></li>
		<li><%= link_to "Terms and Conditions", page_path('terms') %></li>
		<li><%= link_to "Privacy Policy", page_path('privacy') %></li>
	</ul>
</div>
<%= javascript_include_tag "application" %>
</body>
</html>
