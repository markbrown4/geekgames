.page-header
  %h1 Edit profile

= form_for(current_user, :url => profile_path) do |f|
  = f.label :username
  = f.text_field :username
  
  = f.label :country
  = f.country_select :country
  
  .form-actions
    = f.submit :class => 'btn'