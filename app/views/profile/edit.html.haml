- content_for :before_content do
  %h1 Edit profile

.bluey
  = form_for(current_user, :url => profile_path) do |f|
    = f.label :username
    = f.text_field :username
  
    = f.label :country
    = country_selector(f)
  
    .button-area
      = f.submit :value => 'Save'